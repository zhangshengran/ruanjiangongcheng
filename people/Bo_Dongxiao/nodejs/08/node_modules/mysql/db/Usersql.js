var UserSQL = {
    insert: 'INSERT INTO login(uid,userName) VALUES(?,?)', // 插入数据
    drop: 'DROP TABLE login', // 删除表中所有的数据
    queryAll: 'SELECT * FROM login', // 查找表中所有数据
    getUserById: 'SELECT * FROM login WHERE uid =?', // 查找符合条件的数据
};
module.exports = UserSQL;
